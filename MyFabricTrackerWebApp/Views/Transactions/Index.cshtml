@model IEnumerable<MyFabricTrackerWebApp.Models.Transaction>

@{
    ViewData["Title"] = "Adjustments";
}

<h3>Inventory Adjustments</h3>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Image
            </th>
            <th>
                Item #
            </th>
            <th>
                Yardage Qty<br />Adjustment
            </th>
            <th>
                Total<br />Yds
            </th>
            <th>
               Fat Qtr Qty<br />Adjustment
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reason)
            </th>
            <th>
               Date
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @await Html.PartialAsync("_DisplayImageThumbnail", item.Fabric.ImageFileName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fabric.FabricItemCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InchesQty) in
            </td>
            <td>
                @string.Format("{0:n2}", item.Fabric.TotalInches / 36.00)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FatQtrQty)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reason)
            </td>
            <td>
                @item.TransactionDate.ToShortDateString()
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TransactionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TransactionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TransactionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
